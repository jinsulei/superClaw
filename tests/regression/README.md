# Regression Tests

Future responsibility: regression coverage for the eight Hermes 1.0.7 function domains and portable EXE boundaries.

Current status: skeleton only.

Do not add runtime-dependent tests during skeleton work.

Future tests require a separate task and explicit fixtures that exclude secrets and local data.
