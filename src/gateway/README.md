# Gateway

Future responsibility: API routes, event stream, agent bridge, tool bridge, runtime process manager, health check, and security guard.

Current status: skeleton only. Current gateway-like behavior remains in existing files.

Do not directly migrate logic into this directory during skeleton work.

Future migration requires a separate task, contract coverage, and regression verification.
