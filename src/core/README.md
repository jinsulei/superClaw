# Core Runtime

Future responsibility: task framework, permission engine, execution topology, agent dispatcher, observability, audit log, and verification.

Current status: skeleton only. Existing candidates remain in their current paths.

Do not directly migrate logic into this directory during skeleton work.

Future migration requires a separate task, contract coverage, and regression verification.
