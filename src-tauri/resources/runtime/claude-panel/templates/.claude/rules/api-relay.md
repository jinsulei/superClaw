# API Relay Rules

- The product must not contain a built-in API key.
- API keys are optional during initial setup and are provided by the customer.
- API keys must be masked in the frontend and diagnostics.
- Relay configuration writes should remain locked unless administrator authorization is enabled.
- Test connection requests should use minimal requests and must not log complete secrets.

