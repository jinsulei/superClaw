# AGENTS.md Update Proposal

Root `AGENTS.md` already contains active project rules, so this proposal avoids overwriting it.

Suggested short section:

## Hermes 1.0.7 Architecture Navigation

- Before changing Hermes 1.0.7 behavior, read `docs/ARCHITECTURE.md`.
- Before changing a function domain, read `docs/registry/FEATURE_REGISTRY.md`.
- Do not bypass Gateway, Adapter, or Task Framework boundaries.
- Do not package runtime/data/secrets into deliverables.
- Do not continue expanding `scripts/dev-api.js` without a separate migration task.
